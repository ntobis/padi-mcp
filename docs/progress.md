# Progress Log

This file is the persistent memory for the PADI MCP build. Read this and
`docs/discovered-schema.md` first if context was compacted.

Format: `[ISO timestamp] [PHASE] message`. One line per meaningful step.

## 2026-05-26 (Feature 2 — managed service: log catch-up + P5 rate-limit)

- `[2026-05-26T05:05:00Z] [LOG-RECONCILE] The progress log had drifted: its last entry was F2-P4, but main contained two more phases of committed work that were never logged. Reconciled by reading the actual commits on main (post-P4): (1) commit 200865b "Phase 5: tenant-scoped dive tools + connection status on managed MCP endpoint" — touched app/api/[transport]/route.ts + lib/tokens.ts (+test). (2) feature/managed-deploy-and-guard (merged via PR #6, commits ad7fbd3/e7fdd23/ccd1fe3/2df72cf, dated 2026-05-26) — added lib/env-guard.ts (boot-time required-env validation: WORKOS_API_KEY/CLIENT_ID/COOKIE_PASSWORD/AUTHKIT_DOMAIN, PADI_MASTER_KEY, DATABASE_URL), apps/managed vercel-build script (cd ../.. && build:core && next build), and Vercel-build dep fixes (@types/node+typescript on core, vitest as managed devDep). Working branch claude/relaxed-hopper-lvhj5 was a stale ancestor of main (single-user Feature-1 bootstrap only); fast-forwarded it to main (zero-loss, HEAD was an ancestor) so all further work lands on the current tree.`
- `[2026-05-26T05:06:00Z] [F2-P5 STATUS — as-built audit] Confirmed what P5 actually shipped vs design 02 Phase 5 (rate-limit + audit + connection-status): ✅ padi_connection_status tool present in route.ts; ✅ audit_log writes on every mutation (recordWrite → auditLog insert for create/update/delete); ❌ rate-limiting was NEVER built (grep for upstash/ratelimit across the tree hit only the design doc + package-lock, no code). So P5 was ~2/3 done. Also noted (not fixed — out of this task's scope): managed padi_delete_dive has confirm=true but NOT the local server's sandbox guard (MCPTEST_/pre-1950 + iAmSureThisIsNotARealDive override). Flag for a future hardening pass.`
- `[2026-05-26T05:07:00Z] [F2-P6/P7 STATUS] No F2-P6/P7 log entries existed, but deploy is real: the deploy-prep commits (vercel-build, env-guard, Vercel build-green dep fixes) are exactly the iteration you do against a live Vercel deploy, AND the user reports they can add the connector + complete WorkOS AuthKit OAuth from their phone — which is impossible without a live deployed URL + working Layer-1 auth. So F2-P6 (Vercel deploy) is effectively DONE though undocumented; F2-P7 (real-device round-trip) is PARTIAL — caller-OAuth-from-mobile proven; the full "connect PADI account + run a sandbox create/delete from the phone" round-trip is not yet recorded. NOTE: no vercel.json committed — deploy config lives in the Vercel project dashboard settings (root dir + vercel-build), which is valid but means the repo doesn't self-document the deploy.`
- `[2026-05-26T10:20:00Z] [F2-P7 PRE-FLIGHT] Added apps/managed/scripts/p7-preflight.ts (npm run p7-preflight -w @padi-mcp/managed -- <base-url>): checks the DEPLOYED endpoint's unauthenticated Layer-1 discovery contract without secrets — unauth POST /api/mcp → 401 + WWW-Authenticate(resource_metadata), the PRM doc, the AuthKit authorization-server metadata (issuer/jwks_uri/authorization_endpoint/token_endpoint + registration_endpoint for DCR), and /connect not 5xx. Detects a sandbox host-deny (x-deny-reason: host_not_allowed) up front and exits 3 with guidance instead of emitting false "auth not enforced" failures. Deploy URL = https://padi-mcp-managed.vercel.app (MCP endpoint /api/mcp). COULD NOT RUN from the Claude Code sandbox: vercel.app (and the *.authkit.app AuthKit domain that step 3 fetches) are not in this container's network allowlist → host_not_allowed. User must run the pre-flight from their own machine, OR start a fresh session AFTER allowlisting BOTH padi-mcp-managed.vercel.app and the AuthKit domain (allowlist only applies at container start).`
- `[2026-05-26T10:28:00Z] [F2-P7 SANDBOX-DELETE GUARD] Ported the local server's delete guard into the managed tool (apps/managed/app/api/[transport]/route.ts). Added SANDBOX_TITLE_PREFIX='MCPTEST_' + SANDBOX_DATE_CUTOFF='1950-01-01' + isSandboxDive(); padi_delete_dive now takes iAmSureThisIsNotARealDive?:boolean and returns {error:'sandbox_guard',...} when the dive is neither MCPTEST_-titled nor pre-1950 and the override is absent. The override use is recorded in audit_log detail ({sandbox, override}). Mirrors local semantics so deleting from a phone can't nuke a real dive on a bare confirm=true. VERIFIED: managed typecheck clean, 38 managed tests pass, next build (Turbopack) compiles the route. (Guard logic is a direct mirror of the live-verified local isSandboxDive; not separately unit-tested — the route has no handler-level tests yet. On-device P7 will exercise it.)`
- `[2026-05-26T05:10:00Z] [F2-P5 RATE-LIMIT — BUILT] Closed the one genuine P5 code gap. New apps/managed/lib/ratelimit.ts: two per-tenant buckets keyed by the WorkOS user id (NEVER caller input) — 'tool' (default 120/60s, all PADI tool calls) and 'write' (default 20/60s, create/update/delete). Backend = Upstash Redis sliding-window when UPSTASH_REDIS_REST_URL+TOKEN set (durable across Vercel's stateless invocations, lazy dynamic-import so it's only loaded when configured), else an in-memory sliding-window-log fallback (per-instance; fine for local + log-only phase). DECISION (per design's "private phase: generous limits, logging only"): LOG-ONLY by default — over-limit calls are logged to stderr but allowed=true; set RATELIMIT_ENFORCE=1 to flip to blocking (the public-phase switch, no code change). Limits + window + enforce all env-configurable (documented in apps/managed/.env.example). Wired into route.ts: checkRateLimit(userId,'tool') in withTenant (covers all 7 PADI tools), plus checkRateLimit(userId,'write') at the top of create/update/delete; over-limit returns {error:'rate_limited',kind,limit}. Added @upstash/ratelimit@2 + @upstash/redis@1 deps. VERIFIED: 9 new unit tests in test/ratelimit.test.ts (config defaults+overrides+invalid, enforce-flag parse, in-memory sliding-window expiry + key isolation, log-only allows-over-limit, enforce rejects-over-limit, tool/write bucket independence) — all green; full workspace 65 tests pass; managed + root typecheck clean; `next build` (Turbopack) compiles /api/[transport] + /connect successfully. NOTE: Upstash path is NOT unit-tested (needs live Redis) — exercised only via the in-memory fallback; verify against a real Upstash instance during F2-P6 provisioning.`

## 2026-05-25 (Feature 2 — managed service)

- `[2026-05-25T11:35:00Z] [F2-P4 WORKOS AUTHKIT — Layer 1] Caller auth wired. User provisioned WorkOS (env soloworks/Staging, app "padi-mcp", client_01KSFD3R1P9SHXQNT60P6KD4NE; redirect http://localhost:3000/callback + logout http://localhost:3000; Email+Password enabled; Connect→Configuration→MCP Auth: DCR + CIMD enabled). Installed @workos-inc/authkit-nextjs@4 + jose. MCP endpoint now protected by withMcpAuth + verifyWorkosToken (lib/mcp-auth.ts): verifies the WorkOS access-token JWT against the PUBLIC JWKS (https://api.workos.com/sso/jwks/<clientId>) via jose — no secret needed, no profile fetch — and uses payload.sub as the tenant id; tools read userId from extra.authInfo.extra.userId and call getTenantContext. Protected Resource Metadata at app/.well-known/oauth-protected-resource (protectedResourceHandler, authServerUrls=[WORKOS_AUTHKIT_DOMAIN]). Web side: app/callback/route.ts (handleAuth, returnPathname=/connect), proxy.ts (Next 16 — authkit()+handleAuthkitHeaders, matcher /connect/:path*, NOT /api/mcp), /connect action now uses withAuth().user.id (removed the dev-user stub; current-user.ts keeps only connectUrl()). VERIFIED here (no secrets): managed typecheck clean, 18 tests pass (added mcp-auth: no-token/garbage→undefined); dev server — unauth POST /api/mcp → 401 + WWW-Authenticate Bearer ... resource_metadata=.../.well-known/oauth-protected-resource; PRM serves {resource, authorization_servers:[authkit domain]}; /connect+proxy compile (500 only from missing WORKOS_API_KEY in this container, expected). NOTE: mcp-http-smoke (Phase-1 unauth) now 401s by design — test the endpoint via MCP Inspector/Claude with OAuth. NEXT (human/user): full OAuth handshake test locally (needs .env.local WORKOS_* + WORKOS_AUTHKIT_DOMAIN + PADI_MASTER_KEY + DATABASE_URL); then F2-P5 rate-limit/audit/connection-status, F2-P6 deploy (Vercel+Neon+Upstash).`

- `[2026-05-25T10:13:00Z] [F2-P3 CONNECT + TOKEN MINTING] Layer-2 done. lib/connect.ts connectPadiAccount(db,userId,email,password): loginWithPassword → derive affiliate_id/cognito_sub from ID-token claims → envelope-encrypt the refresh token → upsert users + padi_connections (idempotent via onConflict) → audit 'connect' → password discarded (never stored). lib/tokens.ts: getPadiIdToken(db,userId,{forceRefresh}) loads connection, decrypts, mints via refreshTokens, in-memory cache (per userId, refresh when <60s left), on RefreshExpired/NotAuthorized sets status=needs_relogin + throws NeedsReloginError; getTenantContext(db,userId) builds the PadiContext (getToken bound to the tenant). NotConnectedError when no row. DB injected (AppDb = PgDatabase<any,schema>) so logic is testable against PGlite and runs on postgres.js at runtime. lib/current-user.ts: Phase-3 stub getCurrentUserId()='dev-user' (env MANAGED_DEV_USER_ID), connectUrl(). MCP route rewired: padi_count_dives now resolves tenant → getTenantContext → countDives, returning {error:not_connected/needs_relogin, connect_url} gracefully; removed the dev-context env-token stand-in. /connect web flow: app/connect/page.tsx (client, useActionState form) + actions.ts ('use server' connectAction). VERIFIED: 16 managed tests pass (envelope 6, db 3, connect 2 incl. no-password/no-token leakage + idempotent reconnect, tokens 5 incl. mint+cache, forceRefresh re-mint, dead-token→needs_relogin, NotConnected, tenant context); managed typecheck clean; dev server — MCP route compiles, ping/tools-list OK, count correctly reports DATABASE_URL-not-set (DB path wired); /connect renders 200. Added scripts/connect-smoke.ts: live check (PGlite + existing dev refresh token) of decrypt→mint→count, no Postgres/password. NEXT: F2-P4 WorkOS AuthKit (Layer 1) — replaces getCurrentUserId stub with the authenticated session; GATED on WorkOS account.`

- `[2026-05-25T10:05:00Z] [F2-P2 DB + ENCRYPTION] Added the persistence + at-rest-encryption layer to apps/managed. Drizzle schema (lib/db/schema.ts): users (workos_user_id PK), padi_connections (1:1 per user via unique index, FK cascade, stores envelope-encrypted refresh token as base64 text fields enc_refresh_token/enc_nonce/wrapped_dek + affiliate_id/cognito_sub/padi_username/status/timestamps), audit_log. Generated SQL migration via drizzle-kit (drizzle/0000_*.sql, committed). Runtime client lib/db/client.ts = postgres.js + drizzle (prepare:false for Neon pooled), reads DATABASE_URL. Envelope encryption lib/crypto/envelope.ts: per-secret random DEK AES-256-GCM-encrypts the token; DEK wrapped by KEK (PADI_MASTER_KEY, 32 bytes base64 from env); SealedSecret = {ciphertext, nonce, wrappedDek} all base64; documented KMS upgrade path. LOCAL TEST STRATEGY: PGlite (in-process WASM Postgres) instead of docker — applies the real migrations and round-trips data hermetically. VERIFIED: 9 managed tests pass (envelope: round-trip, fresh DEK/nonce each time, no plaintext leak, wrong-KEK fails, GCM tamper detection, key-length validation; db via PGlite: migrations apply, encrypted refresh-token store→read→decrypt, unique-per-user constraint, cascade delete). Managed typecheck clean. NEXT: F2-P3 connect flow (loginWithPassword → encrypt → store; per-request token minting lib/tokens.ts).`

- `[2026-05-25T08:12:00Z] [F2-P1 MCP ENDPOINT] Scaffolded apps/managed (Next.js 16 App Router, React 19) as a workspace. MCP Streamable HTTP endpoint at app/api/[transport]/route.ts via mcp-handler@1.1 + @modelcontextprotocol/sdk@1.26, basePath /api, exports GET/POST/DELETE. Tools: ping (no auth) + padi_count_dives wired to a Phase-1 dev token provider (lib/dev-context.ts) that mints PADI ID tokens from PADI_DEV_REFRESH_TOKEN env (stand-in for the per-tenant provider in P3). Gotcha fixed: Turbopack does not map .js→.ts for relative imports the way tsc bundler does — switched route import to the @/ path alias. VERIFIED: managed typecheck clean; started next dev; a real Streamable HTTP MCP client (SDK StreamableHTTPClientTransport, scripts/mcp-http-smoke.ts) connected to localhost:3000/api/mcp, tools/list returned [ping, padi_count_dives], ping→pong, padi_count_dives→graceful not_configured (no dev refresh token in this container). Dev log shows POST /api/mcp 200s. Local v1 server unaffected: root typecheck/27 tests/build green, stdio still lists 13 tools (SDK deduped to 1.26, backward compatible). NEXT: F2-P2 Postgres + Drizzle schema + envelope encryption (local docker), then P3 connect flow.`

- `[2026-05-25T08:00:00Z] [F2-P0 MONOREPO] Extracted @padi-mcp/core (npm workspaces; user chose npm over pnpm). Moved cognito, jwt, types, transforms, padi-client, and all 7 operations into packages/core/src; refactored the client + operations to take an explicit PadiContext {endpoint, affiliateId, xPlatform, userAgent, getToken} instead of reading a global session — this is the multi-tenant-safe seam (no module-global auth state, safe under Vercel serverless concurrency). padi-client.graphql(ctx, req) now: token via ctx.getToken(); on 401 retries once with ctx.getToken({forceRefresh:true}); throws UnauthorizedError if still 401. Local server stays at repo root (Claude Desktop dist/index.js path unchanged); new src/padi.ts facade binds core ops to a localContext() built from the file session, preserving old single-arg signatures so index.ts + all scripts only changed import paths. logDeletion moved to local src/deletion-log.ts (now best-effort: a write failure no longer turns a successful delete into a reported failure). Build wiring: root tsconfig.json has paths @padi-mcp/core→packages/core/src for typecheck/dev; tsconfig.build.json overrides paths:{} so the app build + node runtime resolve the built core/dist via the workspace symlink; vitest.config.ts aliases core→src; root build script chains build:core then app. session-refresh.test mocks @padi-mcp/core partially (importOriginal + stub login/refresh). VERIFIED: typecheck clean, 27 tests pass, clean rebuild (no stale dist), built server runs from /tmp (cwd-independent), resolves @padi-mcp/core at runtime, 13 tools register, tool calls route facade→core. Live dive ops need a valid session (only on user's machine; logic byte-identical to v1). NEXT: F2-P1 Next.js app + mcp-handler endpoint.`

## 2026-05-24

- `[2026-05-24T08:20:00Z] [P0] Read padi-mcp-brief.md end-to-end. Branch claude/relaxed-hopper-lvhj5 already checked out, clean tree.`
- `[2026-05-24T08:20:30Z] [P0] Found inputs/learning.padi.com-divelog-create-view-edit.har (16MB) — a full HAR capture, not just a single cURL. Plan to write scripts/har-to-session.ts in addition to scripts/curl-to-session.ts so we can extract auth + sample mutations + enum values from it.`
- `[2026-05-24T08:21:00Z] [P0] Created directory structure (docs/, samples/, src/{operations,transforms,tools}/, scripts/, test/fixtures/). Wrote package.json, tsconfig.json, biome.json, .gitignore, .env.example.`
- `[2026-05-24T08:21:30Z] [P0] Copied brief to docs/discovered-schema.md. Created docs/progress.md (this file).`
- `[2026-05-24T08:25:00Z] [P0] Wrote scripts/curl-to-session.ts and scripts/har-to-session.ts. npm install successful (142 packages).`
- `[2026-05-24T08:27:00Z] [P0] Parsed HAR — 50 logbook POSTs. Operations: logbook_units_settings (bonus), logbook_dive_site search, logbook_logs (list+get), logbook_logs_aggregate (count), logbook_depth_time_aggregate (sum bottom_time — bonus), insert_logbook_logs, UpdateRecreationalDiveLog. Saved one sample per op to samples/.`
- `[2026-05-24T08:27:30Z] [P0] HAR Authorization header was stripped by Chrome (expected). session.json written with empty authorization — need cURL paste later for live testing. Continuing with non-auth work.`
- `[2026-05-24T08:28:00Z] [P0] NEW ENUM VALUES from HAR samples (confirmed via captured wire traffic, status 🟡): weather=Cloudy, current=MediumCurrent, cylinder_size=11 (numeric, not enum). Update payload uses numbers (cylinder_size: 11) whereas insert uses strings ("11") — confirms brief's note that API accepts both.`
- `[2026-05-24T08:28:30Z] [P0] Real dive_id 20716851 was used for the captured update — but it's a real recent dive (date 05/24/2026), NOT a sandbox dive, so we must not touch it. Sandbox creation will use MCPTEST_<uuid> + 1900-01-01.`
- `[2026-05-24T08:35:00Z] [P1] Wrote src/session.ts (loader, JWT decode, expiry warnings), src/padi-client.ts (fetch + SessionExpiredError/GraphQLError/HttpError), src/transforms/{dates,numbers}.ts. Verified nowNaiveTimestamp(date) matches captured wire format BYTE-FOR-BYTE: HAR insert created_date="2026-05-24T07:42:39" for request fired at 07:42:39.621Z (UTC wall-clock, no TZ suffix).`
- `[2026-05-24T08:38:00Z] [P2] Wrote src/types.ts (RawDive*, canonical Dive, DiveInput, DiveUpdate) and operations/{list-dives,get-dive,count-dives,search-dive-sites}.ts. All match verbatim queries from brief.`
- `[2026-05-24T08:40:00Z] [P3] Wrote operations/create-dive.ts (string numerics, mirrors HAR insert payload), update-dive.ts (reads existing + merges patch + sends all 5 _set blocks like web UI), delete-dive.ts (3 strategies: hard → per-table → soft, with caching + cleanup).`
- `[2026-05-24T08:42:00Z] [P5] Wrote src/index.ts with 10 tools (ping + 9 padi_*). Enum hints embedded in tool descriptions. Delete tool refuses non-sandbox dives unless iAmSureThisIsNotARealDive=true. Refresh tool accepts raw cURL OR explicit fields.`
- `[2026-05-24T08:42:30Z] [P0] scripts/test-lifecycle.ts (create+diff+update+diff+delete with try/finally cleanup) and scripts/enum-probe.ts (probes all 15 enum fields, appends results to docs/enums.md).`
- `[2026-05-24T08:43:00Z] [P0] DECISION: Moved cURL-parse helper out of scripts/ into src/curl-parser.ts so the MCP server can import it without dragging scripts/ into the build. Added tsconfig.build.json (src→dist only). Resulting bin path is dist/index.js — matches the Claude Desktop config in the brief.`
- `[2026-05-24T08:45:00Z] [P1] All transform unit tests pass (7/7). Clean TS build. Clean typecheck (scripts + tests).`
- `[2026-05-24T08:47:00Z] [P0→P5] MCP server smoke-tested with manual JSON-RPC: initialize OK, tools/list returns all 10 tools with correct JSON schemas, tools/call ping returns pong + session=no session loaded. ping handles missing session gracefully.`
- `[2026-05-24T08:48:00Z] [STOP] Stopping condition reached: need a fresh cURL paste from Chrome DevTools to populate the JWT in inputs/session.json. Without it I cannot verify Phase 1 (count query), Phase 2 (read round-trip), Phase 3 (lifecycle), Phase 4 (enum probe), or Phase 6 (Claude Desktop smoke test). All code is in place; this is a pure "live API access required" block.`
- `[2026-05-24T08:50:00Z] [P1] User pasted cURL. Parsed via scripts/curl-to-session.ts — affiliate_id=14867369, sub=df250b39-eca4-4b74-90a0-ffe3f07a3fed, JWT exp in ~3500s.`
- `[2026-05-24T08:51:00Z] [STOP] HARD BLOCKER: This Claude Code sandbox cannot reach logbook.global-prod.padi.com. Every request returns HTTP 403 with header "x-deny-reason: host_not_allowed" and body "Host not in allowlist". This is NOT a PADI auth failure — the JWT is valid; it's the environment's outbound network policy. Added NetworkPolicyError class to padi-client.ts so future failures of this kind get a clear message instead of being misreported as SessionExpired. User needs to either (a) add logbook.global-prod.padi.com to the environment's network allowlist, or (b) clone the branch and run npm run lifecycle / npm run probe / npm run enum-probe locally on their Mac (the same machine where they captured the cURL). All local code-path tests (transforms 7/7) still pass; the MCP server still starts cleanly.`
- `[2026-05-24T09:02:00Z] [P0] New session on branch claude/brave-mendel-KsUHJ. Network allowlist updated by user — host now reachable (HTTP 301, not 403). Fresh cURL pasted; JWT exp 09:59 UTC (~57min).`
- `[2026-05-24T09:03:00Z] [P1] count probe → 26 dives. ✅ Phase 1 verified live.`
- `[2026-05-24T09:04:00Z] [P2] list 5 + get 20717446 → full read round-trip works. Saved docs/sample-real-dive.json. ✅ Phase 2 verified live.`
- `[2026-05-24T09:05:00Z] [BUG→FIX] First lifecycle run flagged dive_date diff: expected "1900-01-01", actual "1900-01-01T00:00:00". Brief was wrong about read format — the API always returns dive_date as a naive datetime. Added naiveDatetimeToIsoDate() to transforms/dates.ts and routed get-dive + list-dives through it. Unit test added. 11/11 tests pass.`
- `[2026-05-24T09:06:00Z] [P3] Lifecycle PASS 3x consecutively. Delete strategy confirmed: HardDelete always rejected with FK violation (conditions_logs_id_fkey), PerTableDelete always succeeds. Caching means only one wasted HardDelete attempt per cold-start process.`
- `[2026-05-24T09:07:00Z] [P4] First enum-probe run: 64 candidates across 15 fields. Multiple brief assumptions wrong — dive_type: Shore ❌; status: Trash/Deleted/Archived all ❌ (so softDelete default Trash would have failed); suit_type: only Shorty + DrySuit accepted from our 7 candidates; gas_mixture: Nitrox32/36/40 all ❌, Enriched works.`
- `[2026-05-24T09:08:00Z] [P4] Extra enum-probe (scripts/enum-probe-extra.ts) for the gap fields: status adds Pending; gas_mixture adds Nitrox + Heliox; surge adds BigSurge. dive_type and suit_type stayed at 2 values each despite 9 + 12 candidates respectively. Also probed additional_equipment shape → it is a Postgres text[] column with asymmetric wire shape: read=JSON array, write=pg literal string. Anomaly documented.`
- `[2026-05-24T09:09:00Z] [P3→P5] Wired additional_equipment array transform: src/transforms/arrays.ts (toPgTextArray, fromAdditionalEquipment, coerceAdditionalEquipmentWrite). Updated create-dive, update-dive, get-dive. Canonical type is now string[] | null in both directions. 3 new unit tests. Softened delete default to "Draft" since "Trash" is rejected. Updated padi_create_dive tool description with the confirmed enum sets. Lifecycle now also asserts additional_equipment round-trips: PASS.`
- `[2026-05-24T09:11:00Z] [P4-DONE] docs/enums.md rewritten with consolidated accepted table + Notes/gotchas section. docs/anomalies.md gained 5 entries (date format, hard-delete FK, additional_equipment shape, status enum, dive_type+suit_type narrowness). All findings on branch claude/brave-mendel-KsUHJ.`
- `[2026-05-24T09:15:00Z] [P5-DONE] scripts/mcp-smoke.ts: spawns dist/index.js, speaks newline-delimited JSON-RPC, exercises initialize + tools/list + every tool (ping, count, list, search, dry_run, create→get→update→delete) + sandbox-guard rejection. 12/12 passing live. This is as close as we can get to Phase 6 from the sandbox — only Claude Desktop's TUI is missing.`
- `[2026-05-24T09:16:00Z] [STOP] All work that can be done from the sandbox is done. Phase 6 (claude_desktop_config.json install + dictation test from brief line 689) needs the user's Mac. Repo state: phases 0–5 built, 1–5 verified live, README refreshed.`
- `[2026-05-24T09:35:00Z] [P4-AMEND] User uploaded two HAR captures (inputs/moreoptions{1,2}.har) covering the dropdown options that the earlier probes missed. Grepped enum literals → discovered the API uses underscores between letters and numbers (FullSuit_3mm, Enriched_32) plus a "BeachShore" dive_type. scripts/probe-har-enums.ts round-tripped all 9 new values ✅. Also probed the gas_mixture/oxygen interaction (user hint "for some types it's mandatory to provide oxygen mix"): API does NOT enforce consistency — Enriched_36 with oxygen=21 was accepted silently. Tool description now hints that the caller should keep them in sync. docs/enums.md + anomalies.md updated; previous "binary" annotations corrected.`
- `[2026-05-25T05:55:00Z] [P6-BUGFIX] User hit ENOENT on inputs/session.json when running the server inside Claude Desktop. Root cause: SESSION_PATH was resolve(process.cwd(), 'inputs/session.json'), but Claude Desktop launches the server with cwd=/ (or similar), so it looked for /inputs/session.json — unreadable AND the refresh tool's write failed with the same ENOENT. Fix: resolve the path from the module location via fileURLToPath(import.meta.url) (works for both dist/session.js and tsx src/session.ts since inputs/ is one level up from each), plus a PADI_SESSION_PATH env override. replaceSession now mkdir -p's the dir and is best-effort on write (in-memory session stays usable even if persistence fails) and returns {persisted, path}. Verified by launching dist/index.js from /tmp: loads + refreshes to /home/user/padi-mcp/inputs/session.json regardless of cwd. README updated. PR #3 merged.`
- `[2026-05-25T07:25:00Z] [FEATURE-1 COMPLETE + LIVE-VERIFIED] Built the Cognito login + auto-refresh feature per docs/design/01. Shared core: src/auth/cognito.ts (loginWithPassword=USER_PASSWORD_AUTH, refreshTokens=REFRESH_TOKEN_AUTH, plain fetch no AWS SDK, typed CognitoAuthError), src/auth/jwt.ts (claim decode → derives affiliate_id+sub, so no cURL ever needed), src/auth/credential-store.ts (optional pw storage: macOS Keychain else AES-256-GCM file). session.ts now refresh-aware: getValidIdToken() returns live token (refresh when <60s left); mintNewIdToken() does refresh-token → stored-password → SessionNeedsLoginError; backward compatible with legacy id-token-only cURL sessions. padi-client awaits getValidIdToken() per request + 401→forced-refresh→retry-once. New tools padi_login/padi_logout/padi_auth_status (padi_refresh_session kept as fallback). New CLIs: npm run login, npm run verify-refresh. Tests: 27 passing (cognito mocked-fetch, credential-store encrypted round-trip, session refresh state machine incl legacy + password-fallback). LIVE VERIFICATION (Phase 6, human-gated): user ran npm run login (real password, entered locally, never shared) → affiliate_id 14867369, refresh token stored. Then npm run verify-refresh forced REFRESH_TOKEN_AUTH → minted fresh ID token (valid 3599s) → PADI accepted it → counted 29 dives. Full login-once + auto-refresh chain proven on real infra. Commits eb0a4dc + b76bba6 on claude/brave-mendel-KsUHJ. NEXT: Feature 2 (managed service, docs/design/02) when user is ready.`
- `[2026-05-25T06:10:00Z] [DESIGN] Researched + wrote two detailed design docs for the project's two end goals. (1) docs/design/01-local-auth-refresh.md — open-source local build: replace hourly cURL paste with Cognito login-once + auto-refresh; shared Cognito core (plain fetch, no AWS SDK); refresh-token store; optional Keychain password storage; padi_login/logout/auth_status tools + npm run login CLI. (2) docs/design/02-managed-service.md — hosted multi-tenant MCP service on Vercel (mcp-handler + Streamable HTTP) with WorkOS AuthKit for caller auth (Layer 1), Neon Postgres + Drizzle, envelope AES-256-GCM encryption of per-tenant refresh tokens, password-over-TLS connect flow (discard password), per-request token minting w/ Redis cache; monorepo refactor extracting @padi-mcp/core shared by both apps. Verified current stack facts via web research: Vercel mcp-handler + @modelcontextprotocol/sdk@^1.26.0, MCP auth spec (OAuth 2.1 + RFC9728 PRM + RFC7591 DCR), WorkOS AuthKit Vercel MCP template. Both docs specify autonomous build + self-test strategy and enumerate the only human-gated checkpoints (one live PADI login; WorkOS/Vercel/Neon/Upstash provisioning + secrets/domain). Stack decisions from user: Vercel host, low-management auth (→WorkOS), password-over-TLS-discard, build-private-design-for-public. NEXT: implement sequentially, Feature 1 first.`



